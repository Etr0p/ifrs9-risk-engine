[pytest]
testpaths = ifrs9_cockpit/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: tests longs (standalone __main__, reproductibilite) — exclus par defaut
